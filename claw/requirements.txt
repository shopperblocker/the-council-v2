python-telegram-bot>=20.0
# Webhook server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
# All other dependencies are stdlib: json, subprocess, datetime, pathlib, fcntl (Unix)
# Optional: psycopg2-binary (for Railway postgres sync)
# psycopg2-binary>=2.9
